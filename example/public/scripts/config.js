'use strict';

/**
 * This module defines a collection of parameters used throughout the app.
 *
 * @module config
 */
(function () {
  var config = {};

  // TODO: add parameters

  // --- Expose this module --- //

  if (!window.app) window.app = {};
  window.app.config = config;

  console.log('config module loaded');
})();
